{
  "name": "lunatv-config",
  "version": "1.0.0",
  "private": true,
  "description": "Build, validate and monitor LunaTV configuration subscriptions",
  "scripts": {
    "build:configs": "node build-configs.js",
    "encode": "node encode.js",
    "build": "npm run build:configs && npm run encode",
    "check:api": "node check_api.js",
    "update:readme": "node update_readme.js",
    "verify": "node verify.js",
    "test": "node --test worker.test.js check-api.test.js && npm run verify"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "bs58": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
